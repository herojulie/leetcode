from typing import Optional


class KthLargest:
    def __init__(self, k: int, nums: [int]):
        self.k = k
        self.bt = []
        self.make_bt(nums)

    def make_bt(self, nums):
        if not nums:

        root = nums[0]


    def add(self, val: int):


# Your KthLargest object will be instantiated and called as such:
# obj = KthLargest(k, nums)
# param_1 = obj.add(val)
